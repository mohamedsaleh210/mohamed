# سند — تعديلات النسخة v10

تم دمج متطلبات ملف **تعديلات الموقع** على النسخة الأخيرة مع الحفاظ على الموقع الخدمي ونظام المكتب والمنصة المركزية.

## ما تم تنفيذه

- إعادة تنظيم القائمة الجانبية في مجموعات قابلة للفتح والإغلاق مع إبقاء التنبيهات في موضع واضح.
- تطوير لوحة التحكم بمرشحات الفترة والفرع والموظف، وإظهار رصيد الخزنة والإيرادات والمصروفات والعهد والتنبيهات المهمة.
- تطوير الخزنة لتسجيل أكثر من طريقة استلام في العملية نفسها، وإظهار رصيد مستقل لكل طريقة، وإتاحة التحويل والمنصرف والمرفقات والتقارير.
- تطوير تقييم الأداء: الالتزام بالوقت، إنجاز الخطوات، جودة التنفيذ، المستندات وملخص الإغلاق، تقييم المسؤول، نسب مشاركة الموظفين، وإقرار المكافأة أو الخصم بواسطة الإدارة.
- إيقاف احتساب مدة الطلب بسبب معتمد، مع ملاحظة وإثبات اختياري واعتماد واستئناف مسجلين.
- منع إغلاق الطلب قبل إنجاز الخطوات ورفع المستندات وكتابة ملخص الإغلاق.
- إضافة نظام دعم فني كامل: تذاكر، أولوية، حالات، مسؤولون متعددون، ردود وملاحظات داخلية ومرفقات وتقييم وإعادة فتح وتقارير وصلاحيات.
- تطوير صفحة النظام للمكاتب والشركات: مساران واضحان، باقات شهرية وسنوية، مقارنة، رحلة استخدام، أسئلة شائعة وطلب تجربة.
- إدارة أسعار وحدود الباقات من لوحة مالك سند المركزية بدل تثبيتها داخل الصفحة.
- إضافة صفحة من نحن وتحسين وصف الصفحات والتنقل ودعم الهاتف وإمكانية الوصول.
- توسيع الصلاحيات لتشمل الدعم الفني وكل العمليات الجديدة؛ يمكن منح كل صلاحية أو سحبها لكل موظف.

## قرارات تنظيمية

- رصيد الخزنة هو رصيد حالي تراكمي، بينما الإيرادات والمصروفات تتبع الفترة المختارة في لوحة التحكم.
- المكافآت والخصومات مقترحة من نتيجة التقييم، ولا تُعتمد ماليًا تلقائيًا إلا بعد موافقة الإدارة.
- «الاستشارات» الحالية ما زالت تدير طلبات الاستشارة. لم يُنشأ دليل مستقل باسم «المستشارون» لأن الملف لم يحدد هل المقصود موظفين داخليين أم خبراء خارجيين؛ يمكن إضافته لاحقًا عند تحديد المقصود.
- أسعار الباقات والحدود والضريبة قابلة للتعديل من لوحة المالك المركزية، ولا تعتمد النسخة على أسعار نهائية مفروضة.

## التشغيل المحلي

يتطلب Node.js 20 أو 22 LTS.

```bash
npm install
npm run demo
npm start
```

- الموقع ونظام المكتب: `http://localhost:3000`
- الدخول الموحد: `http://localhost:3000/login`
- لوحة الإدارة: `http://localhost:3000/office-panel/login`

لتجربة سند المركزية في نافذة أوامر أخرى:

```bash
npm run platform
```

- المنصة المركزية: `http://localhost:4000`
- لوحة المالك: `http://localhost:4000/owner/login`
- بيانات أول تشغيل محلي: `sanad-owner` / `ChangeMe123!`

> غيّر كلمات المرور والأسرار قبل النشر الفعلي، واستخدم HTTPS حتى يعمل تسجيل الموقع الجغرافي للموظف خارج localhost.
