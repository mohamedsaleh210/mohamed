# سند v15.4 — بيانات تجريبية كاملة

يضيف هذا الإصدار سيناريو عرض مترابطًا للمكتب ولوحة مالك منصة سند.

## التشغيل

```bash
npm install
npm run demo
npm start
```

ولتشغيل لوحة مالك المنصة في نافذة أوامر أخرى:

```bash
npm run platform
```

- موقع المكتب: `http://localhost:3000`
- لوحة المكتب: `http://localhost:3000/office-panel/login`
- لوحة مالك سند: `http://localhost:4000/owner/login`

بيانات الدخول التجريبية التفصيلية تُكتب في `TEST-ACCOUNTS.txt` بعد تشغيل أمر
التجهيز. هذه الحسابات للاختبار المحلي فقط وليست للإنتاج.

## نطاق البيانات

- طلبات وعملاء وموظفون وصلاحيات وإشعارات ومستندات وخطوات تنفيذ.
- شركات وفروع وجهات اتصال وخدمات متعددة على الطلب الواحد.
- قضايا وجلسات ومهام وأطراف وأجندة ومواعيد.
- أتعاب ودفعات ومصروفات وخزنة وعهد ورواتب وتقييمات أداء.
- تذاكر دعم ورسائل وأحداث وتقييم حل التذكرة.
- قوالب تقارير وسجل تصدير وسجل استيراد ورسائل بريد تجريبية.
- باقات وطلبات تجربة واشتراك بكل الحالات، وثلاثة حسابات مكاتب: تجريبي ونشط وموقوف.
- صلاحيات خدمات وعناصر ظهور مختلفة لكل مكتب.

## التحقق

```bash
npm run test:demo
npm run test:imports
npm test
```

إعادة تشغيل `npm run demo` آمنة للعرض: يتم حذف سجلات المحاكاة المعرفة وإعادة
بنائها، ولا تتضاعف الصفوف في كل تشغيل.
