# سند v16.4.2 — إصلاحات أمان وموثوقية قبل التجربة المحلية

بناءً على المراجعة التشخيصية الكاملة، هذه أول دفعة إصلاحات — كلها إصلاحات
لعيوب فعلية في السلوك (ليست حذفًا أو إعادة تصميم لأي وحدة أو خدمة). لم يُحذف
أي عنصر، ولم يتغيّر أي مسار أو صلاحية أو تصميم.

## ما تم إصلاحه

### 1. حساب مالك سند لم يعد يُعاد ضبطه عند كل تشغيل (P0-1)
- **الملفات:** `db/seed.js`, `platform/db.js`
- **قبل:** كل إعادة تشغيل للخادم كانت تعيد كلمة مرور `adam` إلى
  `Mas@123456789` تلقائيًا، حتى لو غيّرها المالك بنفسه.
- **بعد:** يحدث هذا **مرة واحدة فقط** عند أول تشغيل (ويُسجَّل ذلك في
  `settings` / `platform_meta`). أول دخول بعدها **يُلزم بتغيير كلمة
  المرور**. أي تشغيل لاحق يترك كلمة المرور كما ضبطها المالك.
- يمكن تخصيص القيمة الأولى عبر `PLATFORM_ADMIN_USERNAME` و
  `PLATFORM_ADMIN_PASSWORD` في البيئة، بدل الاعتماد على القيمة الافتراضية.

### 2. بيانات المكتب لم تعد "تختفي" عند نقل المشروع لجهاز آخر (P0-2)
- **الملفات:** `platform/db.js`, `platform/launcher.js`
- **قبل:** مسار بيانات كل مكتب (`data_dir`) كان محفوظًا كمسار مطلق من جهاز
  التطوير. تشغيل النسخة على أي جهاز آخر كان يجعل النظام يظن أن المسار غير
  موجود وينشئ قاعدة بيانات **فارغة جديدة** بدل فتح البيانات الحقيقية.
- **بعد:** عند كل تشغيل، يتحقق النظام من كل مكتب: لو المسار المسجَّل غير
  موجود على هذا الجهاز لكن مجلد `platform-data/tenants/<slug>` موجود،
  يُستخدم هذا المجلد ويُصحَّح المسار في القاعدة تلقائيًا (مرة واحدة). بيانات
  شركة الجرواني المرفقة ستظهر الآن بشكل صحيح.

### 3. صفحة الدخول الموحدة صارت محمية من محاولات كلمة المرور غير المحدودة (P0-3)
- **الملف:** `routes/unified_login.js`
- **قبل:** `/login` (نقطة دخول الموظفين والعملاء معًا) لم تكن تطبّق حد
  المحاولات (8 محاولات / 15 دقيقة) المطبَّق على `/office-panel/login`، ولا
  تُسجِّل محاولات الدخول في سجل الأمان.
- **بعد:** نفس آلية الحد وتسجيل السجل الأمني المستخدمة في لوحة الإدارة.

### 4. مرفقات الدعم الفني لم تعد متاحة بلا تسجيل دخول (P1-5)
- **الملفات:** `server.js`, `routes/support-files.js` (جديد)
- **قبل:** أي رابط مرفق دعم فني (`/support-files/...`) كان يُقرأ مباشرة بلا
  أي تحقق من الهوية — يشمل ذلك مرفقات **الردود الداخلية** التي لا يُفترض أن
  يراها العميل إطلاقًا.
- **بعد:** نفس الرابط، لكن الآن يتحقق من الجلسة: أي موظف مسجّل دخوله يرى
  المرفق (كما هو الحال في لوحة الإدارة أصلًا)، والعميل يرى فقط مرفقات ردوده
  غير الداخلية على تذاكره هو.

### 5. إجماليات الخزنة في الفترة المفلترة أصبحت دقيقة (P1-4)
- **الملف:** `routes/admin/treasury.js`
- **قبل:** «الوارد» و«المنصرف» في أعلى الشاشة كانا يُحسبان من آخر 1000
  حركة معروضة فقط — صحيح بالصدفة إذا كانت الفترة أقل من 1000 حركة، وخطأ
  صامت (رقم أقل من الحقيقي) بعدها.
- **بعد:** يُحسبان بواسطة SQL على كل الحركات المطابقة للفلتر، بصرف النظر
  عن عدد الصفوف المعروضة. رصيد كل خزنة (`balance()`) لم يتأثر أصلًا لأنه
  كان صحيحًا من قبل.

### 6. «المتبقي» في لوحة التحكم أصبح متسقًا مع شاشتي العملاء والإيرادات
- **الملف:** `routes/admin/index.js`
- **قبل:** لوحة التحكم كانت تحسب المتبقي كـ
  `إجمالي − مدفوع` فقط، متجاهلة الخصم والإعدام، وبلا فلترة حسب صلاحية
  رؤية الطلبات (خلاف عدّادات الطلبات في نفس الصفحة).
- **بعد:** نفس الصيغة المستخدمة في شاشة العملاء والإيرادات
  (`إجمالي − خصم − إعدام − مدفوع`، بحد أدنى صفر لكل طلب)، ومفلترة بنفس
  نطاق الرؤية والفرع/الموظف المُطبَّق على باقي أرقام الصفحة.

## ما لم يُلمَس عمدًا (يحتاج قرارك أولًا)

هذه فُحصت في التقرير التشخيصي ولم تُعدَّل، لأنها قرارات تصميم/محتوى وليست
أخطاء بحتة:

- عزل فروع المكتب (`office_branch_id`) — P1-1
- رحلة الحجز المركزية — P1-2
- نموذج النشر بالمنافذ — P1-3
- المحتوى التسويقي الملفّق في الصفحة الرئيسية — P0-4
- ربط العهد/السلف بالرواتب — P1-7
- نص المانيفست المضلِّل — P1-8
- مصروفات لا تتطلب طلبًا — يحتاج قرارك
- نص صلاحيات التصدير (يعد بـExcel/PDF وينفّذ CSV/طباعة) — يحتاج قرارك

## بيانات الدخول لهذه النسخة (أول تشغيل فقط)

- مالك سند (لوحة `/office-panel` أو `npm run platform`): `adam` /
  `Mas@123456789` — **سيُطلب منك تغييرها فور أول دخول**.
- مدير شركة الجرواني (بعد تشغيل `npm run platform` وفتح مساحة الشركة):
  `elgrwanylaw` / `Snd@123456` — لم يتغيّر، هذا الحساب لا تلمسه هذه الدفعة.
