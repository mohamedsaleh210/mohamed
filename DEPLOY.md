# رفع سند على Hostinger

خطتك **Unlimited** — وهي الاسم الجديد لخطة **Business**، وبتدعم **٥ مواقع Node.js**.
مش محتاج ترقية ولا VPS.

**Express.js** من الفريمووركات المدعومة رسمياً، وإصدارات Node المتاحة: 18 · 20 · 22 · 24.

---

## ⚠️ اقرا دي قبل ما تبدأ

Hostinger بتحط ملفات التطبيق في:

```
/home/<username>/domains/<domain>/nodejs
```

وأي deploy جديد **بيستبدل المجلد ده بالكامل**.

قاعدة البيانات وملفات العملاء **لازم يكونوا بره المجلد ده**، وإلا أول تحديث
بيمسح شغل شهور. الشكل الصح:

```
/home/username/
├── domains/<domain>/nodejs/   ← الكود (بيتبدل كل deploy)
└── sanad-data/                ← الداتابيز والملفات (ثابت — متلمسوش)
```

المشروع فيه حماية: لو `DATA_DIR` طلع جوّه مجلد المشروع والوضع `production`،
بيطبع تحذير كبير في اللوج. **لو شفته، وقّف وظبّط المسار.**

---

## الخطوات

### ١. مجلد البيانات

مش محتاج تعمله بإيدك — حط `DATA_DIR=~/sanad-data` في المتغيرات
والتطبيق بينشئه في المكان الصح أول تشغيل.

### ٢. ارفع الكود على GitHub

```bash
cd sanad
git init
git add .
git commit -m "Sanad"
git branch -M main
git remote add origin https://github.com/<حسابك>/sanad.git
git push -u origin main
```

`.gitignore` بيمنع رفع `node_modules` و `data/` و `.env` و `TEST-ACCOUNTS.txt`.
**خلّي الريبو private** — فيه منطق النظام كله.

### ٣. أنشئ الموقع في hPanel

**Websites → Add Website → Deploy Web App → Import Git Repository**

بعدها **Authorize** لـ GitHub، واختار ريبو `sanad`.

> لو الدومين مضاف قبل كده كموقع عادي، لازم **تحذف الموقع القديم الأول** —
> Hostinger مش بتحوّل موقع موجود لتطبيق Node. خد نسخة احتياطية قبل الحذف.

### ٤. إعدادات البناء

المفروض يكتشف **Express.js** لوحده. تأكد من:

| الخانة | القيمة |
|---|---|
| Framework | `Express.js` (أو `Other` لو مكتشفهاش) |
| Node version | **20** |
| Install command | `npm install` |
| Build command | `npm run build` (موجود ومش بيعمل حاجة) |
| Start command | `npm start` |
| Entry file | `server.js` |
| Output directory | **سيبها فاضية** |

سند بيرندر على السيرفر مباشرة — **مفيش build ولا مجلد ناتج**. لو الحقل
إجباري، حط `.` وخلاص.

### ٥. متغيرات البيئة

من داشبورد الموقع → **Environment Variables**:

```
NODE_ENV=production
SESSION_SECRET=<ولّده بالأمر تحت>
ADMIN_PATH=/اختار-مسار-خاص
DATA_DIR=~/sanad-data
```

> **مش إجباري.** لو سيبته فاضي، التطبيق في وضع الإنتاج بيحط الداتا في
> `~/sanad-data` لوحده — بره مجلد المشروع، فالنشر مبيوصلهاش. حطه بنفسك بس
> لو عايز مكان مختلف.

> `~` معناها مجلد حسابك، والتطبيق بيحوّلها لوحده — فمش محتاج تعرف اسم
> المستخدم. المسار الكامل `/home/<username>/sanad-data` بيشتغل برضه.

توليد المفتاح:
```bash
node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
```

`PORT` **متحطهوش** — Hostinger بتحدده لوحدها والتطبيق بيقراها.

> السيرفر **بيرفض يشتغل** في `production` من غير `SESSION_SECRET`.
> ده مقصود: أهون إنه ميشتغلش من إنه يشتغل بمفتاح موجود في الكود.

### ٦. Deploy

دوس **Deploy** وتابع اللوج. أول مرة بتاخد دقيقتين-تلاتة بسبب `better-sqlite3`.

أول تشغيل بيعمل كل حاجة لوحده: بينشئ الجداول، وبيحط الأقسام والخدمات
الـ ٢٢، وبيعمل حساب `adam`. **مش محتاج تشغّل أي أمر.**

### ٧. أول ١٠ دقايق بعد ما يشتغل

1. افتح `https://<دومينك><ADMIN_PATH>/login` — دخول `adam` / `1234`
2. **غيّر كلمة السر فوراً** من «حسابي»
3. **الإعدادات → الموقع**: الدومين (بالـ https) والواتساب والعملة
4. **الإعدادات → الإيميل**: مفتاح Resend وإيميل المُرسِل وإيميلات المكتب
5. **الموظفين**: اعمل حسابات الفريق
6. **حسابات التواصل**: فيسبوك وإنستجرام وغيرهم
7. تأكد إن `TEST-ACCOUNTS.txt` **مش موجود على السيرفر**

---

## التحديثات بعد كده

```bash
git add .
git commit -m "التعديل"
git push
```

Hostinger بتبني وتنشر لوحدها مع كل push. وفيه زرار **Restart** في الداشبورد
لو احتجت تعيد تشغيل السيرفر من غير build كامل.

**قبل أي push كبير، خد نسخة احتياطية:**
```bash
tar -czf ~/backup-$(date +%F).tar.gz ~/sanad-data
```

---

## النسخ الاحتياطي التلقائي

**hPanel → Advanced → Cron Jobs**، مرة كل يوم الساعة ٣ الفجر:

```bash
mkdir -p ~/backups && tar -czf ~/backups/sanad-$(date +\%F).tar.gz ~/sanad-data && find ~/backups -name "sanad-*.tar.gz" -mtime +14 -delete
```

بيحتفظ بآخر ١٤ يوم ويمسح الأقدم.

> SQLite بيكتب ملفات `-wal` و `-shm` جنب الداتابيز. **خد المجلد كامل**
> مش ملف `.db` لوحده، وإلا النسخة هتبقى ناقصة.

---

## لو حصلت مشكلة

**البناء فشل عند `better-sqlite3`**
تأكد إن نسخة Node **20** مش 24. المكتبة بتحمّل نسخة جاهزة للإصدارات
المستقرة، ومع الإصدارات الجديدة أوي بتحاول تبني من الأول وبتفشل.

**الموقع بيدي 403 بعد الـ deploy**
ملف `.htaccess` في `public_html` هو اللي بيوجّه الطلبات لمجلد `nodejs`.
أعِد الـ deploy — بيتولّد من جديد.

**الصور بتترفع بحجمها الأصلي**
يعني `sharp` مثبتتش. الموقع شغال عادي بس من غير ضغط وتحويل HEIC.
شوف اللوج، هتلاقي تحذير. حلّها بإعادة deploy، ولو فضلت، شيلها من
`optionalDependencies` واشتغل من غيرها.

**الداتا ضاعت بعد deploy**
`DATA_DIR` كان جوّه مجلد المشروع. رجّع من النسخة الاحتياطية وظبّط المسار.
**افحص ده من أول deploy قبل ما يبقى فيه عملاء.**

**رفع ملف كبير بيفشل**
حد الرفع عندنا ١٥ ميجا للملف. الصور بتتضغط في المتصفح قبل ما تتبعت،
فالمفروض متوصلش للحد ده أصلاً.

---

## حاجات تخص خطتك

- **٥ مواقع Node** — سند واحد منهم
- **٣ جيجا رام · ٢ نواة** — أكتر من كفاية
- **٥٠ جيجا مساحة** — الطلب ≈ ٣ ميجا بعد الضغط، يعني **~١٥ ألف طلب**
- **إرسال الإيميل من السيرفر محدود** بـ ١٠/دقيقة و١٠٠/يوم — **مش بيمسّنا**،
  لأن سند بيبعت عن طريق Resend API مش عن طريق السيرفر
- **npm مش بيشتغل من SSH** على الخطة دي — بس مش محتاجينه،
  كل حاجة بتتظبط لوحدها أول تشغيل

---

## قائمة قبل التسليم

- [ ] كلمة سر `adam` اتغيّرت
- [ ] `SESSION_SECRET` متظبط ومتغيّر
- [ ] `ADMIN_PATH` مش القيمة الافتراضية
- [ ] `NODE_ENV=production`
- [ ] `DATA_DIR` بره مجلد المشروع — **واتأكدت إن مفيش تحذير في اللوج**
- [ ] الدومين متكتوب في الإعدادات (اللينكات في الإيميلات بتتبني منه)
- [ ] `TEST-ACCOUNTS.txt` مش على السيرفر
- [ ] الريبو private
- [ ] SSL شغّال (بيتظبط لوحده)
- [ ] النسخ الاحتياطي اليومي متظبط
- [ ] جرّبت طلب حقيقي من الموقع ووصل للوحة
