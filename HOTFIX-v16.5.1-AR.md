# Sanad v16.5.1 — Startup Hotfix

هذا الإصدار يصحح خطأ بدء التشغيل في migration رقم 056.

- السبب: ملف `056_login_throttle.js` كان يصدّر دالة مباشرة بينما مشغّل migrations يتوقع `exports.up`.
- الإصلاح: تصدير `exports.up = (db) => { ... }` بما يتوافق مع بقية ملفات migrations.
- أضيف سكربت `npm run dev` كمرادف محلي لـ `npm start`.
- لا توجد أي تغييرات على قواعد البيانات المرفقة أو بيانات شركة الجرواني.
