# سند v16.0 — النسخة الاحترافية التجريبية

## نطاق النسخة

هذه النسخة مبنية فوق بيانات v15.4.1 الأصلية وتحديثات الواجهة v15.6.1، من دون إنشاء قاعدة فارغة أو استبدال بيانات الموظفين والعملاء والطلبات.

## ما كان عليه المشروع

- بعض أقسام الصفحة الرئيسية موجودة في القالب لكنها مخفية من قاعدة البيانات وقواعد CSS متأخرة.
- صورة الواجهة والعنوان والأزرار الرئيسية مكتوبة مباشرة داخل القالب.
- البحث في الصفحة الرئيسية يرسل كلمات البحث، لكن صفحة الخدمات لا تنفذ بحثًا فعليًا.
- نقل حزمة المنصة إلى مجلد أو خادم آخر قد يترك مسارات قواعد المكاتب تشير إلى مكان النسخة القديمة.
- تشغيل مساحات المكاتب كان يجبر وضع `development` حتى عند تشغيل المنصة في الإنتاج.
- اختبار الحالات الحدّية يتوقف عند حذف عميل مرتبط، وتنظيف بيانات التجربة لا يغطي العلاقات التي أضافتها الوحدات الجديدة.

## ما أصبح عليه

- تطبيق الترتيب البصري المعتمد وإظهار أقسام: البحث، المؤشرات، فئات العملاء، المكاتب، الخدمات، خطوات العمل، لماذا سند، سند للأعمال، الباقات، الآراء والأسئلة.
- مركز تحرير للواجهة داخل: `لوحة الأدمن ← محتوى الصفحة الرئيسية`.
- تحكم في نصوص الواجهة الرئيسية بالعربية والإنجليزية، الأزرار وروابطها وصورة الواجهة ووصفها.
- بحث فعلي في أسماء الخدمات ووصفها، مع تصفية حسب القطاع ونتائج مرتبطة بطلب الخدمة.
- استمرار التحكم في إظهار الأقسام وترتيبها والمؤشرات والخدمات المثبتة والآراء والأسئلة.
- إصلاح قابلية نقل مساحات المكاتب؛ المسارات تُعاد إلى مجلد الحزمة الحالية بأمان عند وجود النسخة المحمولة.
- تشغيل المكاتب بوضع البيئة الفعلي وعدم إجبار `development`.
- توسيع سياسة حذف العميل التجريبية وتنظيف بيانات العرض مع الحفاظ على السجلات القانونية والمالية المرتبطة.
- الحفاظ على عزل الصلاحيات، إخفاء البيانات المالية، حماية الملفات، CSRF وسياسات كلمات المرور.

## حوكمة المنصة

- أدمن سند هو المالك الأعلى للمنصة.
- أدمن سند يعتمد طلب المكتب أو الشركة ويحدد الباقة والمدة والحدود.
- أدمن سند ينشئ أو يعتمد أول مدير للمكتب.
- مدير المكتب يدير موظفيه وصلاحياتهم داخل الحدود الممنوحة.
- الموظف يرى الأعمال المصرح بها فقط.
- العميل يرى طلباته وملفاته ومدفوعاته فقط.
- بيانات كل مكتب مستقلة عن المكاتب الأخرى.

## الحفاظ على البيانات

الأعداد عند التسليم:

- المستخدمون والموظفون: 38
- العملاء: 33
- الشركات: 3
- الطلبات: 80
- الخدمات: 82
- التصنيفات: 19
- الدفعات: 87
- المصروفات: 54

توجد نسخة احتياطية قبل ترقية v16 داخل `backups/pre-v16`.

## نتائج الاختبارات

- الاختبار الشامل: 963 نجاح، 0 فشل.
- رحلات التكامل: 74 نجاح، 0 فشل.
- فحص الأمن: 138 نجاح، 0 فشل.
- الحالات الحدّية: 178 نجاح، 0 فشل.
- سلامة SQLite: `ok`.
- مخالفات العلاقات: 0.

## التشغيل المحلي

1. ثبّت Node.js 18 أو أحدث.
2. افتح مجلد المشروع.
3. شغّل `npm install` إذا لم يكن مجلد `node_modules` موجودًا.
4. شغّل `npm start`.
5. افتح `http://localhost:3000`.
6. دخول لوحة الإدارة: `http://localhost:3000/office-panel/login`.

راجع `TEST-ACCOUNTS.txt` لبيانات الحسابات الموجودة. يجب تغيير كلمات المرور المؤقتة قبل النشر الفعلي.

## ملاحظة معمارية صريحة

هذه النسخة Pilot احترافية قابلة للتجربة وتحافظ على النظام الحالي المبني على SQLite. التحويل النهائي إلى PostgreSQL وتشغيل SaaS واسع النطاق على تطبيق واحد وعزل `tenant_id` يظل مرحلة نشر مؤسسية مستقلة؛ لا يصح الادعاء بأنه تم بمجرد تحسين الواجهة أو إبقاء حزمة `pg` ضمن الاعتماديات.
