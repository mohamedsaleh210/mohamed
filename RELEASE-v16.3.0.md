# Sanad v16.3.0 — Payroll Excel

## الجديد

- قالب Excel مستقل لكل سند صرف مرتب، مُعبأ تلقائيًا بموظفي الشهر والرقم القومي والقيم الحالية.
- معاينة إلزامية قبل التطبيق توضح الصفوف الصحيحة والمرفوضة والفروقات المالية.
- تحديث الموظف الموجود فقط؛ لا ينشئ الاستيراد موظفين أو بنودًا مكررة.
- حساب إجمالي الاستحقاقات والخصومات وصافي المرتب داخل الخادم، وليس اعتمادًا على معادلات الملف.
- ترحيل سند شهر سابق إلى شهر جديد مع نقل البنود الثابتة فقط.
- تصفير البنود المتغيرة عند الترحيل: المكافآت والحوافز والإضافي والغياب والتأخير والجزاءات والسلف والخصومات الأخرى.
- تسجيل تطبيق ملف Excel وترحيل الشهر في سجل النشاط.
- اعتماد المصطلحات المصرية في الواجهات: «رقم الموبايل»، «الرقم القومي»، و«سند صرف مرتب».

## قاعدة البيانات

- Migration: `054_payroll_excel_import.js`
- يضيف جدول `payroll_import_batches` لحفظ معاينات الاستيراد وحالتها وبصمة الملف والمستخدم الذي طبقها.
- الترحيل إضافي وآمن ولا يحذف أو يعدل السجلات الحالية.

## التشغيل

1. فك الضغط في مجلد جديد.
2. نفّذ `npm install`.
3. شغّل `npm start`.
4. افتح لوحة الإدارة ثم: **الموظفون ← المرتبات والمكافآت**.
5. أنشئ أو افتح سندًا في حالة «مسودة» لتنزيل قالب Excel أو ترحيله لشهر جديد.

## اختبارات الإصدار

- Payroll Excel: PASS — القالب، المعاينة، التطبيق مرة واحدة، منع التكرار، والترحيل.
- Appointments E2E: PASS 48 / FAIL 0.
- Integration: PASS 74 / FAIL 0.
- Security: PASS 138 / FAIL 0.
- SaaS P0 controls: PASS 17 / FAIL 0.
- Main regression: PASS 968، مع ملاحظة قديمة واحدة غير مرتبطة بهذا الإصدار تخص إجبار جميع حسابات المحاكاة على تغيير كلمة السر في وضع الإنتاج.

## حدود مقصودة

- الاستيراد متاح لسندات المسودة فقط.
- الملفات المقبولة: XLSX وXLSM حتى 5MB.
- لا يتم إنشاء موظف من Excel؛ يجب أن يكون الموظف موجودًا مسبقًا في سند الشهر.
- لا يمكن تطبيق ملف يحتوي على صف مرفوض، ويجب تصحيحه ثم رفعه من جديد.
