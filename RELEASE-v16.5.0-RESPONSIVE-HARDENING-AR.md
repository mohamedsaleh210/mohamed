# سند v16.5.0 — Responsive & Hardening

## نطاق الإصدار
هذا الإصدار يحافظ على بنية Express/EJS الحالية وقواعد البيانات ومسارات الأعمال، ويضيف إصلاحًا Mobile‑First للواجهة مع مجموعة إصلاحات أمان وسلامة بيانات منخفضة المخاطر.

## أبرز إصلاحات الواجهة
- Mobile Header حقيقي مع Off‑canvas Drawer بدل ضغط قائمة سطح المكتب.
- دعم المقاسات 320/360/375/390/393/414/430/768/1024 وDesktop.
- RTL/LTR للـDrawer واتجاه العناصر المشتركة.
- Safe Area لأجهزة iPhone، وزر WhatsApp مضغوط ولا يظهر أثناء فتح القائمة.
- منع Horizontal Overflow على مستوى الصفحة.
- تحسين Forms/Buttons/Dialogs/Grids للمس والشاشات الصغيرة.
- Sidebar الإدارة يبقى Desktop كما هو ويتحول Drawer على Mobile.
- تحويل جداول الإدارة إلى Cards على الهاتف مع توليد data-label تلقائيًا من رؤوس الأعمدة.
- دعم table-scroll المحلي للجداول الكثيفة بدل تحريك الصفحة كاملة.

## إصلاحات الأمان والبيانات
- Login throttle مستمر داخل SQLite بدل Map مؤقت يختفي مع Restart.
- منع تشغيل Production بكلمة مرور مالك افتراضية إذا لم يتم تحديد PLATFORM_ADMIN_PASSWORD.
- تشديد الوصول إلى صور هوية الموظفين إلى users.manage.
- إخفاء مؤشرات وشهادات العملاء التجريبية من الصفحة العامة دون حذفها من القاعدة.
- إزالة ربط صريح باسم شركة الجرواني من منطق الخدمات العامة وجعله عامًا للشركات المفعلة.
- توحيد متطلب كلمة مرور طلب الاشتراك مع سياسة كلمة المرور المركزية.
- إضافة روابط النسخ الاحتياطية وبطاقات الدخول إلى قائمة مالك المنصة.

## إصلاحات الخزنة
- الرصيد والإجماليات تعتمد الحركات المعتمدة فقط.
- منع الصرف بما يتجاوز الرصيد للجميع، بما في ذلك الأدمن.
- الصرف من مستخدم لا يملك settings.manage يصبح Pending ويحتاج اعتمادًا.
- إضافة اعتماد/رفض مع Audit Log.
- إجماليات الفلاتر تُحسب من جميع الصفوف في SQL وليس أول 1000 صف.
- الشاشة قد تعرض أول 1000 صف للأداء مع تنبيه صريح؛ CSV والطباعة تشمل جميع النتائج المطابقة.

## التحقق المنفذ
- فحص syntax لجميع ملفات JavaScript باستخدام node -c: ناجح.
- فحص توازن قوالب EJS: ناجح.
- اختبار Chromium باستخدام CSS الفعلي على 320/360/375/390/393/414/430/768/1024/1366، في RTL وLTR: لا Horizontal Overflow في Fixtures العامة والإدارية، والـDrawers داخل الشاشة.
- مقارنة قاعدة Tenant الخاصة بالجرواني قبل/بعد: SHA-256 مطابق ولم تتغير.
- مقارنة أعداد الصفوف في الجداول التشغيلية الأساسية: لم تتغير. التغيير المقصود فقط هو visible للبيانات التسويقية التجريبية.

## ملاحظات تحقق البيئة
تعذر تشغيل حزمة npm الكاملة في بيئة الإنشاء لأن تثبيت better-sqlite3 الأصلي لم يكتمل داخل بيئة التنفيذ. لذلك لا يُسجل هذا الإصدار على أنه اجتاز npm test الكامل هنا. فحوصات syntax، قواعد البيانات، وقواعد Responsive/Chromium المذكورة أعلاه اجتازت.

## عناصر معمارية لم تُغيّر عمدًا في هذا الإصدار
هذه تحتاج مرحلة منفصلة لأنها تغيّر سلوك النظام لا مجرد إصلاح آمن:
- عزل الرؤية حسب office_branch_id لكل دور/موظف.
- تحويل Central Appointments من redirect إلى رحلة حجز مركزية كاملة.
- Production tenant routing عبر reverse proxy/subdomains بدل المنافذ المحلية.
- توسيع نموذج الاستشارات بسعر/مدة/مستشار/فيديو/عاجلة.
- Availability لكل موظف/فرع داخل booking_slots.
- استكمال pagination وaudit coverage في جميع الوحدات القديمة.

لا تُغيّر هذه النسخة نموذج قاعدة مستقلة لكل Tenant، ولا lib/permissions.js، ولا middleware/auth.js، ولا lib/csrf.js، ولا lib/data-import.js، ولا lib/backup.js، ولا lib/bookings.js إلا ما ذُكر صراحة.
