# سند v16.5.2 — بطاقة دخول الموظف

إضافة الإجراءات المطلوبة إلى **بطاقة دخول الموظف الموجودة داخل ملف الموظف** دون تغيير قواعد البيانات أو مسارات النظام الأساسية.

## الجديد

- QR Code داخل البطاقة يفتح صفحة دخول الجهة، ويظهر كذلك عند الطباعة.
- زر **تحميل PDF**: ينشئ ملف PDF فعلي للبطاقة.
- زر **طباعة**.
- زر **إرسال بالبريد** باستخدام نظام البريد الموجود في سند؛ إذا لم يكن مزود البريد مفعلاً تُنشأ الرسالة في `mail-outbox` للتجربة بدلاً من إظهار نجاح وهمي.
- زر **إرسال SMS** عبر Webhook قابل للتهيئة.
- زر **نسخ رابط الدخول**.
- زر **إعادة إصدار كلمة المرور** مع تأكيد، ويُبطل كلمة المرور المؤقتة السابقة.
- تعرض البطاقة: اسم الموظف، الشركة/المكتب، الفرع، رابط الدخول، اسم المستخدم، كلمة المرور المؤقتة، البريد والجوال إن وُجدا.
- الإجراءات لا تظهر في النسخة المطبوعة/PDF، بينما يظهر QR والتنبيه الأمني.
- جميع إجراءات الإرسال/PDF تتحقق أن كلمة المرور ما زالت هي الكلمة المؤقتة الحالية ولم يتم تغييرها أو استبدالها.

## إعداد SMS

لإرسال SMS فعلي يجب ضبط أحد الخيارين:

- `SMS_API_URL` أو الإعداد `sms_api_url`
- اختياري: `SMS_API_KEY` / `sms_api_key`
- اختياري: `SMS_SENDER` / `sms_sender`

يرسل سند طلب POST بصيغة JSON:

```json
{"to":"...","message":"...","sender":"SANAD"}
```

وإذا لم يكن المزود مضبوطًا تظهر رسالة واضحة أن خدمة SMS غير مفعلة ولا يتم ادعاء نجاح الإرسال.

## حماية البيانات

لم تُعدّل أي قاعدة بيانات مرفقة في هذا الإصدار. تم التحقق من SHA-256 لقواعد `central.db` وقاعدة شركة الجرواني و`data/sanad.db` مقابل v16.5.1 وكانت متطابقة.
