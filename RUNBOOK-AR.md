# تشغيل ونشر منصة سند

## المتطلبات

- Node.js 20 أو 22 LTS موصى به؛ الحد الأدنى 18.
- مساحة كتابة دائمة لمجلدي البيانات.
- HTTPS في الاستضافة.

## التشغيل المحلي

```bash
npm ci
cp .env.example .env
# عدّل المسارات والأسرار في .env ثم حمّلها بالطريقة المناسبة لنظامك
npm start
```

يفتح الموقع على `http://localhost:3000`. لوحة الإدارة في المسار الموجود في `ADMIN_PATH`. لتشغيل لوحة المنصة المركزية منفصلة:

```bash
npm run platform
```

## أول تشغيل

1. أنشئ سرين عشوائيين مختلفين لـ `SESSION_SECRET` و`PLATFORM_SESSION_SECRET`.
2. اجعل `DATA_DIR` و`PLATFORM_DATA_DIR` مسارين دائمين خارج مجلد الكود.
3. ضع اسم وكلمة مرور قوية لمالك المنصة قبل أول تشغيل إنتاجي.
4. اترك `SEED_DEMO=0`.
5. شغّل `npm ci` ثم `npm start`؛ تُطبق ترحيلات قاعدة المكتب تلقائيًا دون حذف البيانات.
6. ادخل كمدير واضبط الهوية والتواصل والبريد وGoogle عند الحاجة.

## النشر

1. ارفع ملفات المشروع من دون `node_modules` أو مجلدات البيانات المحلية.
2. عرّف متغيرات `.env.example` في لوحة الاستضافة، ولا ترفع ملف `.env`.
3. ثبّت الاعتمادات بـ `npm ci --omit=dev`.
4. استخدم `npm start` كأمر التشغيل.
5. اربط مجلدي البيانات بوحدة تخزين دائمة وخذهما في نسخة احتياطية دورية.
6. اضبط Reverse Proxy إلى `PORT` وفعل HTTPS.
7. بعد النشر شغّل فحص قبول: الدخول، طلب جديد، رفع ملف، دفعة، فاتورة، إغلاق وتقييم.

## النسخ الاحتياطي والاستعادة

أوقف الكتابة مؤقتًا أو أوقف الخدمة، ثم انسخ كامل `DATA_DIR` و`PLATFORM_DATA_DIR` بما فيه ملفات `db-wal` و`db-shm` إن وُجدت. للاستعادة أوقف الخدمة واستبدل المجلدين بنسخة متطابقة ثم أعد التشغيل.

## أوامر التحقق

```bash
npm run test:all
npm audit --omit=dev
```

## متغيرات البيئة

راجع `.env.example`. الأسرار لا توضع في الكود أو مستودع Git. `SANAD_PROVISION` و`TENANT_ID` متغيران داخليان ينشئهما النظام لمساحات الشركات ولا يُضبطان يدويًا.
