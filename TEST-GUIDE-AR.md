# دليل تجربة سند محليًا

## الرابط الذي تبدأ منه

بعد التشغيل افتح فقط:

`http://localhost:3000`

من الصفحة الرئيسية تستطيع طلب خدمة، طلب نسخة تجريبية، أو الضغط على «تسجيل الدخول».
لا تحتاج إلى حفظ روابط الأجندة أو الاشتراكات أو لوحة الإدارة؛ تظهر هذه الصفحات من القائمة حسب صلاحية الحساب.

## التشغيل لأول مرة على Windows

1. فك الملف المضغوط في مجلد جديد.
2. افتح المجلد، واكتب `cmd` في شريط عنوان مستكشف الملفات ثم اضغط Enter.
3. نفّذ الأوامر التالية بالترتيب:

```bat
npm install
npm run demo
npm start
```

4. اترك النافذة السوداء مفتوحة، ثم افتح `http://localhost:3000`.

في مرات التشغيل التالية يكفي:

```bat
npm start
```

## حسابات التجربة من زر تسجيل الدخول

| الدور | اسم الدخول | كلمة المرور |
|---|---|---|
| مدير النظام | `adam` | `1234` |
| مشرف | `nour` | `demo1234` |
| موظف/محامٍ | `khaled` | `demo1234` |
| محاسب | `samia` | `demo1234` |
| عميل | `client@demo.sanad` | `demo1234` |

## رحلة الاختبار المقترحة

1. افتح الموقع وتصفّح الخدمات وصفحة نظام المكاتب والشركات.
2. قدّم طلب خدمة جديد كعميل، واحتفظ برقم الطلب.
3. ادخل بحساب المدير `adam` وافتح الطلب من لوحة التحكم.
4. غيّر الحالة، عيّن أكثر من موظف، أضف أتعابًا وخطوات تنفيذ وموعدًا.
5. ادخل بحساب الموظف `khaled` وتأكد من ظهور المهمة والإشعار.
6. ادخل بحساب العميل وتأكد من ظهور الحالة والأتعاب والتحديثات المسموح بعرضها.
7. ادخل بحساب المحاسب `samia` وراجع المصروفات والإيرادات والعهد.
8. من حساب المدير راجع الأجندة والعملاء والشركات والفروع والاشتراكات والهوية المالية.

## تجربة طلب نسخة مكتب

من الصفحة الرئيسية افتح «خدمات المكاتب والشركات»، ثم أرسل طلب نسخة تجريبية.
ادخل بعد ذلك بحساب المدير `adam`، وافتح «الاشتراكات» من القائمة، ثم وافق على الطلب وحدد مدة التجربة والباقات.

## إيقاف النظام

ارجع إلى نافذة الأوامر واضغط `Ctrl + C`.

> تنبيه: `npm run demo` يعيد إنشاء بيانات المحاكاة. لا تشغله بعد إدخال بيانات حقيقية تريد الاحتفاظ بها.
